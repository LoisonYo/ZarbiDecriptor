from cv2 import cv2
import numpy as np

class ZarbiRotator:
    def process(self, img):
        #img = cv2.bitwise_not(img)
        #cv2.imshow("invert", img)
        #print(np.sum(img, axis=1))
        return img